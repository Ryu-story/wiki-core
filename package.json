{
  "name": "wiki-core",
  "private": true,
  "version": "0.1.0",
  "description": "wiki-core — common abstraction for domain-specific wiki systems (rootric / plott / enroute via plugin model)",
  "scripts": {
    "build": "pnpm -r build",
    "typecheck": "pnpm -r typecheck",
    "clean": "pnpm -r exec rm -rf dist && rm -rf dist-tarballs",
    "pack:dist": "node scripts/pack-dist.mjs"
  },
  "devDependencies": {
    "typescript": "^5.5.0"
  },
  "packageManager": "pnpm@9.0.0",
  "engines": {
    "node": ">=20"
  }
}
